unit uFrmPrincipal;

interface

uses
  Winapi.Windows, Winapi.Messages, System.SysUtils, System.Variants, System.Classes, Vcl.Graphics,
  Vcl.Controls, Vcl.Forms, Vcl.Dialogs, REST.Types, REST.Client,
  Data.Bind.Components, Data.Bind.ObjectScope, Vcl.StdCtrls, System.Types, System.StrUtils;

type
  TFrmPrincipal = class(TForm)
    Memo1: TMemo;
    Button1: TButton;
    RESTClient1: TRESTClient;
    RESTRequest1: TRESTRequest;
    RESTResponse1: TRESTResponse;
    procedure Button1Click(Sender: TObject);
  private
    { Private declarations }
  public
    { Public declarations }
  end;

var
  FrmPrincipal: TFrmPrincipal;

implementation

{$R *.dfm}

procedure TFrmPrincipal.Button1Click(Sender: TObject);
  var
    i: integer;
    retornoString: string;
    //retornoVetor: TStringDynArray;
    //retorno: string;

begin
  Memo1.Clear;
  RESTRequest1.Execute;
  retornoString := RESTRequest1.Response.JSONText;
  Memo1.Lines.Add(retornoString.Substring(0,length(retornoString)-2));
  //  delete(retornoString,0,length(retornoString)-1);

  //retornoVetor := SplitString(retornoString, ',');
  //Memo1.Lines.Add(retornoVetor[0]);
  //Memo1.Lines.Add(retornoVetor[1]);
  //Memo1.Lines.Add(retornoVetor[2]);
  //Memo1.Lines.Add(retornoVetor[3]);
end;

end.
